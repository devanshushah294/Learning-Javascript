// window object is a global object
// It represents the browser's window.
// All global JavaScript objects, functions, and variables automatically become members of the window object.
// Global functions are methods of the window object.
console.log(window);
console.log(document);
console.log(document.body);
document.body.style.backgroundColor = "yellow";

// Browser object model is a additional objects provided by a browser for working with everything exxcept document
// alert confirm and prompt are the part of BOM
